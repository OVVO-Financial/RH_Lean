name: Lean verification

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: lean-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Fix one-time canonical occupancy TeX escape
        if: github.repository == 'OVVO-Financial/RH_Lean' && github.head_ref == 'agent/analysis-canonical-low-occupancy'
        run: |
          python scripts/fix_canonical_low_paper_escape.py
          if ! git diff --quiet -- paper/Squared_Complex_Framework_for_Square_Prefix_Mobius_Sums_Lean_Complete.tex; then
            git config user.name "OVVO Lean Automation"
            git config user.email "actions@users.noreply.github.com"
            git add paper/Squared_Complex_Framework_for_Square_Prefix_Mobius_Sums_Lean_Complete.tex
            git commit -m "Fix canonical low occupancy paper escape"
            git push origin HEAD:${{ github.head_ref }}
          fi

      - name: Check paper / Analysis boundary
        run: bash scripts/check_paper_analysis_boundary.sh

      - name: Audit unfinished proofs and axioms
        run: bash scripts/audit_assumptions.sh

      - name: Build Lean project
        uses: leanprover/lean-action@v1
        with:
          use-mathlib-cache: true
          build: true
          build-args: "RHLean --wfail"
          test: false
          lint: false
