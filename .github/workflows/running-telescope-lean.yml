name: Running-state matched frontier telescope check

on:
  push:
    branches:
      - agent/full-sequential-canonical-frontier-bridge
      - agent/global-energy-telescope
    paths:
      - RHLean/Proof/SquareRootLowPrimeRunningTelescope.lean
      - RHLean/Proof/SquareRootLowPrimeGlobalEnergyTelescope.lean
      - RHLean/Proof/SquareRootLowPrimeMatchedFrontierBound.lean
      - RHLean/Proof/SquareRootLowPrimeQuantitativeEnergyReduction.lean
      - RHLean/Proof/SquareRootLowPrimeSignedResponseMatching.lean
      - .github/workflows/running-telescope-lean.yml
  workflow_dispatch:

permissions:
  contents: write
  statuses: write

concurrency:
  group: running-telescope-lean-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Check out branch
        uses: actions/checkout@v4

      - name: Import telescope modules
        run: |
          python3 - <<'PY'
          from pathlib import Path
          manifest = Path('RHLean.lean')
          targets = {
              'import RHLean.Proof.SquareRootLowPrimeSignedResponseChildren',
              'import RHLean.Proof.SquareRootLowPrimeSignedResponseMatching',
              'import RHLean.Proof.SquareRootLowPrimeQuantitativeEnergyReduction',
              'import RHLean.Proof.SquareRootLowPrimeMatchedFrontierBound',
              'import RHLean.Proof.SquareRootLowPrimeRunningTelescope',
              'import RHLean.Proof.SquareRootLowPrimeGlobalEnergyTelescope',
          }
          lines = [line.strip() for line in manifest.read_text().splitlines() if line.strip()]
          manifest.write_text('\n'.join(sorted(set(lines) | targets)) + '\n')
          PY
          if ! git diff --quiet -- RHLean.lean; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add RHLean.lean
            git commit -m "Import global energy telescope"
            git push
          fi

      - name: Audit source and manifest
        run: |
          bash scripts/check_paper_analysis_boundary.sh
          bash scripts/audit_assumptions.sh
          python3 scripts/check_root_manifest.py

      - name: Restore Mathlib packages
        uses: actions/cache/restore@v5
        with:
          path: .lake/packages
          key: lake-packages-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}

      - name: Set up Lean and Mathlib
        uses: leanprover/lean-action@v1
        with:
          use-github-cache: false
          use-mathlib-cache: true
          build: false
          test: false
          lint: false

      - name: Apply StrongPNT compatibility patch
        run: |
          python3 scripts/strongpnt_424/apply.py
          python3 scripts/strongpnt_424/apply_post.py
          python3 scripts/strongpnt_424/apply_pnt5_mid.py
          python3 scripts/strongpnt_424/apply_pnt5_strong.py
          python3 scripts/strongpnt_424/apply_lint.py

      - name: Restore StrongPNT build cache
        uses: actions/cache/restore@v5
        with:
          path: |
            .lake/packages/PrimeNumberTheoremAnd/.lake/build
            .lake/packages/StrongPNT/.lake/build
          key: strongpnt-port-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-${{ hashFiles('scripts/strongpnt_424/*.py') }}
          restore-keys: |
            strongpnt-port-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-

      - name: Prebuild StrongPNT dependency
        run: lake build StrongPNT.PNT5_Strong

      - name: Restore RHLean build cache
        uses: actions/cache/restore@v5
        with:
          path: .lake/build
          key: rhlean-build-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json', 'scripts/strongpnt_424/*.py', 'RHLean.lean', 'RHLean/**/*.lean') }}
          restore-keys: |
            rhlean-build-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json', 'scripts/strongpnt_424/*.py') }}-
            rhlean-build-${{ runner.os }}-${{ runner.arch }}-

      - name: Build telescope chain
        id: build
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          lake build \
            RHLean.Proof.SquareRootLowPrimeSignedResponseChildren \
            RHLean.Proof.SquareRootLowPrimeSignedResponseMatching \
            RHLean.Proof.SquareRootLowPrimeQuantitativeEnergyReduction \
            RHLean.Proof.SquareRootLowPrimeMatchedFrontierBound \
            RHLean.Proof.SquareRootLowPrimeRunningTelescope \
            RHLean.Proof.SquareRootLowPrimeGlobalEnergyTelescope \
            2>&1 | tee running-telescope.log

      - name: Commit diagnostics on failure
        if: ${{ steps.build.outcome == 'failure' }}
        run: |
          cp running-telescope.log research/running-telescope-build-errors.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add research/running-telescope-build-errors.txt
          git commit -m "Record running telescope compiler diagnostics"
          git push

      - name: Publish status
        if: ${{ always() }}
        uses: actions/github-script@v7
        with:
          script: |
            const failed = '${{ steps.build.outcome }}' === 'failure';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: failed ? 'failure' : 'success',
              context: 'running-telescope-lean',
              description: failed
                ? 'Running-state frontier telescope failed'
                : 'Running-state frontier telescope compiled',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
            });

      - name: Fail on build error
        if: ${{ steps.build.outcome == 'failure' }}
        run: exit 1
