name: Canonical liberty compiled theorem check

on:
  push:
    branches: [agent/canonical-mobius-liberty-core]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: canonical-liberty-packet-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache/restore@v5
        with:
          path: .lake/packages
          key: lake-packages-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}
      - uses: leanprover/lean-action@v1
        with:
          use-github-cache: false
          use-mathlib-cache: true
          build: false
          test: false
          lint: false
      - name: Apply StrongPNT compatibility patch
        run: |
          python3 scripts/strongpnt_424/apply.py
          python3 scripts/strongpnt_424/apply_post.py
          python3 scripts/strongpnt_424/apply_pnt5_mid.py
          python3 scripts/strongpnt_424/apply_pnt5_strong.py
          python3 scripts/strongpnt_424/apply_lint.py
      - name: Audit source and manifest
        run: |
          bash scripts/check_paper_analysis_boundary.sh
          bash scripts/audit_assumptions.sh
          python3 scripts/check_root_manifest.py
      - uses: actions/cache/restore@v5
        with:
          path: |
            .lake/packages/PrimeNumberTheoremAnd/.lake/build
            .lake/packages/StrongPNT/.lake/build
          key: strongpnt-port-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}-${{ hashFiles('scripts/strongpnt_424/*.py') }}
          restore-keys: |
            strongpnt-port-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lakefile.lean', 'lake-manifest.json') }}-
      - name: Prebuild StrongPNT dependency
        run: lake build StrongPNT.PNT5_Strong
      - name: Kernel-check compiled liberty theorems
        run: |
          lake build \
            RHLean.Proof.SquareRootLowPrimeCanonicalLibertyPartition \
            RHLean.Proof.SquareRootLowPrimePartialPacketBoundary \
            RHLean.Proof.SquareRootLowPrimeCanonicalCreationResponseMap \
            RHLean.Proof.SquareRootLowPrimeNoTogglePopulationBound \
            RHLean.Proof.SquareRootLowPrimeHighSquareClosure \
            RHLean.Proof.SquareRootLowPrimeBornSquareBoundary \
            RHLean.Proof.SquareRootLowPrimeCanonicalFailureRoot \
            RHLean.Proof.SquareRootBornPostTailLowPrimeRemainder
