name: Dyadic survivor Mertens invariant

on:
  pull_request:
    branches: [main]
    paths:
      - RHLean/Proof/DyadicSurvivorMertensInvariant.lean
      - RHLean/Analysis/DyadicTransportCompression.lean
      - RHLean/Proof/PrimeCombReciprocalBandCancellation.lean
      - RHLean/Analysis/SquareRootMatchedTransport.lean
      - .github/workflows/dyadic-survivor-mertens.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Restore Lake packages
        uses: actions/cache/restore@v5
        with:
          path: .lake/packages
          key: lake-packages-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}
      - uses: leanprover/lean-action@v1
        with:
          use-github-cache: false
          use-mathlib-cache: true
          build: false
          test: false
          lint: false
      - name: Apply StrongPNT 4.24 compatibility patch
        run: |
          python3 scripts/strongpnt_424/apply.py
          python3 scripts/strongpnt_424/apply_post.py
          python3 scripts/strongpnt_424/apply_pnt5_mid.py
          python3 scripts/strongpnt_424/apply_pnt5_strong.py
          python3 scripts/strongpnt_424/apply_lint.py
      - name: Build dependency
        run: lake build StrongPNT.PNT5_Strong
      - name: Compile survivor invariant
        run: lake env lean -DwarningAsError=true RHLean/Proof/DyadicSurvivorMertensInvariant.lean
