After the terminal closure in SquareEndpointQ2Amplification.lean, the remaining arithmetic target is square-endpoint only:

For X_R = R^2 - 1, Y_q = floor(X_R/q^2), s_q = floor(sqrt(Y_q)), prove after the full signed #674 reconstruction that

  M(X_R)^2 <= C R^2 K + 4 * sum_{q<R, q odd prime} M(s_q^2 - 1)^2

for every LowerMertensCriticalEnvelope R K.

The short shells M(Y_q)-M(s_q^2-1), finalQ2SurvivorCorrection, and the already bounded final root boundary must remain signed until they are absorbed into C R^2 K.

The terminal induction then has exact recursive coefficient 4*(17/72)=17/18 < 1 and yields SquareRootMertensEndpointAmplificationStatement, hence the existing x^epsilon closure to RH.
