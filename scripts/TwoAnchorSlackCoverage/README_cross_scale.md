Run `packet_cross_scale.c` after the merged top-fibre gatekeeper when testing any
q-fibre signed grouping. The script computes exact integer Gram data, global locality
`eta_w`, dynamic top-row locality, signed tail total variation, contiguous-block
inflation, and a numeric audit of the prime-boundary identity.

The current finite record is in `packet_cross_scale_results.md`; the route decision is
in `packet_cross_scale_decision.md`.
